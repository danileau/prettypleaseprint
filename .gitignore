node_modules/
.next/
out/
build/
.env
.env*.local
.env.docker
data/
*.log
.DS_Store
next-env.d.ts
*.tsbuildinfo
.prisma/
/storage
shots/
.env.backup

# Swagger UI, copied out of node_modules by `npm run vendor:swagger` (which
# `predev` and `prebuild` run for you). Somebody else's build output — the
# package manager owns it, not the repository.
public/docs/

# Agent worktrees and other local Claude state — never committed.
.claude/
